# Required for live model calls (OpenAI-compatible)
OPENAI_API_KEY=
# Optional OpenRouter / custom base URL
# OPENAI_BASE_URL=https://openrouter.ai/api/v1
# HELIX_MODEL=gpt-4.1-mini

# Persistent state directory (default: ./.helix)
# HELIX_HOME=.helix
